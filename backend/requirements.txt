# BE-008: Updated 2026-02-20 — bumped all packages, fixed CVEs in cryptography
# BE-009: Updated 2026-04-15 — CVE fixes: cryptography, pyjwt, requests, pyasn1
# BE-010: Updated 2026-06-18 — CVE fixes: python-multipart 0.0.31 (CVE-2026-53538/53539/53540), cryptography 48.0.1 (GHSA-537c-gmf6-5ccf)
# BE-011: Updated 2026-06-29 — CVE fixes: fastapi+starlette (CVE-2025-54121, CVE-2025-62727,
#   CVE-2026-48818, CVE-2026-48817, CVE-2026-54283, CVE-2026-54282, PYSEC-2026-161,
#   PYSEC-2026-248, PYSEC-2026-249), paramiko (CVE-2026-44405) — closes #308
# Core framework
fastapi==0.138.1
uvicorn==0.34.0
websockets==14.1
pyyaml==6.0.2
python-multipart==0.0.31
pydantic-settings==2.7.1

# Database — SQLite via the stdlib driver. Phase 4 dropped PostgreSQL (no
# server for a single-user local tool) and Alembic with it: the schema is
# created from the ORM models at startup, see backend/database.py.
sqlalchemy==2.0.36

# Security — cryptography 50.x fixes PYSEC-2026-3552/3553/3554 (48.0.1 was affected;
# 3553/3554 need >=49, 3552 needs >=50). Used for encrypting kubeconfigs and cloud
# credentials at rest; bnkscope has no authentication of its own (Phase 3).
cryptography==50.0.0
PyJWT==2.13.0
pyasn1==0.6.4

# Scheduling — periodic jobs run in-process on APScheduler. The broker and
# result backend they used to need went with Celery in Phase 4; see
# backend/core/background.py for what replaced the worker pool. croniter went
# with them: every job here is a fixed interval, not a cron expression.
apscheduler==3.10.4

# Cloud and cluster access
boto3==1.36.4
google-auth==2.38.0
kubernetes==31.0.0
kr8s==0.20.15

# gRPC — NICo's Forge API is gRPC-only (no CRDs, no REST). The schema is read
# off the server with reflection rather than vendored, so grpcio-reflection is
# here for its client stubs and protobuf for the dynamic descriptor pool.
# See backend/services/nico/forge.py.
grpcio==1.83.0
grpcio-reflection==1.83.0
protobuf==7.36.0

# HTTP. h2 is not optional here: nico-api multiplexes its HTTP root onto the
# same HTTP/2 listener as gRPC, so the version banner is unreadable over
# HTTP/1.1 (see services/nico/fetch.py:_version_banner).
httpx==0.28.1
h2==4.4.1
requests==2.33.0

# Process metrics (CPU/RAM/network for self-monitoring endpoint)
psutil==6.1.1
