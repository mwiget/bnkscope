[project]
name = "bnkscope-mcp"
version = "0.1.0"
description = "MCP Server for bnkscope — AI-accessible tools for F5 BIG-IP Next for Kubernetes"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [{ name = "bnkscope Team" }]

dependencies = [
    # Capped below 2: the 2.x major removed `mcp.server.fastmcp`, which
    # server.py imports. An uncapped `>=1.26.0` resolved to 2.0.0 as soon as it
    # was published, which broke the image (ModuleNotFoundError at startup, so
    # bnkscope-mcp crash-looped) and the test job together, on a commit that
    # changed neither. Lifting the cap means porting off mcp.server.fastmcp.
    "mcp>=1.26.0,<2",
    "httpx>=0.27.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-cov>=4.0",
    "ruff>=0.4.0",
    "respx>=0.21.0",
]

[project.scripts]
bnkscope-mcp = "bnkscope_mcp.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bnkscope_mcp"]

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
