# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║  BNK-Forge v2 — Release Pipeline                                           ║
# ║                                                                             ║
# ║  Triggers:                                                                  ║
# ║    • push to staging  → pre-release RC tag (no VERSION commit)             ║
# ║    • push to main     → final release (VERSION bump + CHANGELOG commit)    ║
# ║    • workflow_dispatch → manual override (keeps the original manual path)  ║
# ║                                                                             ║
# ║  Version derivation: conventional commits since last final vX.Y.Z tag      ║
# ║    feat! / BREAKING CHANGE → major                                         ║
# ║    feat                    → minor                                          ║
# ║    fix / other             → patch                                          ║
# ║                                                                             ║
# ║  Infinite-loop guard: commits starting with "release: " have [skip ci]    ║
# ║  appended and are filtered out by the head-commit check below.             ║
# ╚══════════════════════════════════════════════════════════════════════════════╝

name: Release

on:
  push:
    branches:
      - main
    # MUST stay in sync with ci.yml's push-trigger paths-ignore list — a
    # divergence lets a push trigger Release without a matching CI run,
    # which then times out the preflight SHA poll below (PR #297 review).
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.agent/**'
      - '.opencode/**'
      - 'LICENSE'
      - '.gitignore'
      - '.trivyignore'
      - 'USER_GUIDE.md'
  workflow_dispatch:
    inputs:
      version_bump:
        description: "Version bump type"
        required: true
        default: "patch"
        type: choice
        options:
          - patch   # 2.10.49 → 2.10.50
          - minor   # 2.10.49 → 2.11.0
          - major   # 2.10.49 → 3.0.0
      release_notes:
        description: "Release notes (one-line summary)"
        required: true
        type: string

concurrency:
  group: release-${{ github.ref_name }}
  cancel-in-progress: false  # Never cancel a release in progress

jobs:
  # ── Guard: skip release: commits on main (prevents infinite loop) ────────────
  guard:
    name: "Loop guard"
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.check.outputs.should_run }}
    steps:
      - name: Check head commit
        id: check
        env:
          HEAD_COMMIT_MSG: ${{ github.event.head_commit.message }}
        run: |
          MSG="$HEAD_COMMIT_MSG"
          # Skip if triggered by our own release commit or [skip ci] sentinel
          if echo "$MSG" | grep -qE '^release: |^\[skip ci\]|\[skip ci\]$'; then
            echo "Skipping: head commit is a release commit or has [skip ci]"
            echo "should_run=false" >> "$GITHUB_OUTPUT"
          else
            echo "should_run=true" >> "$GITHUB_OUTPUT"
          fi

  # ── Pre-flight: verify CI passed + derive version ────────────────────────────
  preflight:
    name: "Pre-flight Checks"
    needs: guard
    if: needs.guard.outputs.should_run == 'true'
    runs-on: ubuntu-latest
    # `actions: read` is load-bearing: the CI-status poll below calls
    # `gh run list`, and this repo's default workflow permission is "read",
    # which grants contents/metadata/packages only -- actions is `none`. The
    # poll's `2>/dev/null || echo ""` then turns the resulting 403 into an
    # empty result, so it reports "No CI run found yet" every 30s for the full
    # 45-minute budget and fails, even though CI passed on that exact SHA.
    permissions:
      contents: read
      actions: read
    outputs:
      current_version: ${{ steps.version.outputs.current }}
      new_version: ${{ steps.version.outputs.new }}
      bump_type: ${{ steps.version.outputs.bump_type }}
      release_kind: ${{ steps.kind.outputs.kind }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0  # Full history + tags needed for conventional-commit derivation

      - name: Determine release kind
        id: kind
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "kind=manual" >> "$GITHUB_OUTPUT"
          elif [ "${{ github.ref_name }}" = "main" ]; then
            echo "kind=final" >> "$GITHUB_OUTPUT"
          else
            echo "kind=rc" >> "$GITHUB_OUTPUT"
          fi

      - name: Check CI status on this branch
        run: |
          BRANCH="${{ github.ref_name }}"
          SHA="${{ github.sha }}"
          echo "Checking CI status for branch '$BRANCH' at commit $SHA"

          # Match the CI run by the exact commit SHA that triggered this
          # release, not by "--limit=1 --branch=X" (which is racy: on main,
          # ci.yml and release.yml fire on the same push so --limit=1 can
          # return this-same-push's still-running CI run; on staging, a
          # branch-name lookup can return a stale run from an unrelated
          # earlier push). ci.yml has a push:staging trigger specifically so
          # every staging push gets its own CI run carrying that push's SHA.
          # Budget: last five green CI runs on this repo took ~18 min
          # end-to-end; 2700s (45 min) covers queue delays and future growth.
          TIMEOUT_SECONDS="$CI_POLL_TIMEOUT_SECONDS"
          POLL_INTERVAL=30
          ELAPSED=0

          while true; do
            # stderr is deliberately NOT discarded. It used to be, and a 403
            # from a missing `actions: read` scope was indistinguishable from
            # "the run has not appeared yet" -- 90 identical poll lines and a
            # 45-minute timeout with the real cause never printed once.
            if ! RUN_JSON=$(gh run list \
              --workflow=ci.yml \
              --branch="$BRANCH" \
              --limit=20 \
              --json headSha,status,conclusion,databaseId \
              --jq "[.[] | select(.headSha == \"${SHA}\")] | sort_by(.databaseId) | last"); then
              echo "::error::gh run list failed -- see the error above. This is an" \
                   "API/permissions failure, not a missing run."
              exit 1
            fi

            if [ -z "$RUN_JSON" ] || [ "$RUN_JSON" = "null" ]; then
              echo "[${ELAPSED}s] No CI run found yet for commit $SHA on branch '$BRANCH'"
            else
              RUN_STATUS=$(echo "$RUN_JSON" | jq -r '.status')
              RUN_CONCLUSION=$(echo "$RUN_JSON" | jq -r '.conclusion')
              echo "[${ELAPSED}s] CI run status=$RUN_STATUS conclusion=$RUN_CONCLUSION"

              if [ "$RUN_STATUS" = "completed" ]; then
                if [ "$RUN_CONCLUSION" = "success" ]; then
                  echo "CI has passed for commit $SHA on branch '$BRANCH'"
                  exit 0
                fi
                if [ "$RUN_CONCLUSION" = "cancelled" ]; then
                  # ci.yml runs with cancel-in-progress: true, so a rapid
                  # follow-up push to the same branch cancels this SHA's CI
                  # run. That's not a CI failure for this SHA — it means a
                  # newer push superseded it, so fail fast instead of
                  # reporting a generic non-success error.
                  echo "::error::CI run for $SHA was cancelled — superseded by a newer push; this release attempt is stale, the newer push will release instead."
                  exit 1
                fi
                echo "::error::CI run for commit $SHA on branch '$BRANCH' completed with conclusion '$RUN_CONCLUSION'."
                exit 1
              fi
            fi

            if [ "$ELAPSED" -ge "$TIMEOUT_SECONDS" ]; then
              echo "::error::Timed out after ${TIMEOUT_SECONDS}s waiting for a completed CI run matching commit $SHA on branch '$BRANCH'."
              echo "Re-run this workflow once the CI run for this commit finishes: https://github.com/${{ github.repository }}/actions/workflows/ci.yml"
              exit 1
            fi

            sleep "$POLL_INTERVAL"
            ELAPSED=$((ELAPSED + POLL_INTERVAL))
          done
        env:
          GH_TOKEN: ${{ github.token }}
          CI_POLL_TIMEOUT_SECONDS: 2700

      - name: Derive version from conventional commits
        id: version
        run: |
          chmod +x scripts/compute_version_bump.sh

          RELEASE_KIND="${{ steps.kind.outputs.kind }}"
          CURRENT=$(cat VERSION)
          echo "current=$CURRENT" >> "$GITHUB_OUTPUT"

          if [ "$RELEASE_KIND" = "manual" ]; then
            # Manual override: use the dropdown input directly
            IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT"
            case "${{ inputs.version_bump }}" in
              major) NEW="$((MAJOR + 1)).0.0" ;;
              minor) NEW="${MAJOR}.$((MINOR + 1)).0" ;;
              patch) NEW="${MAJOR}.${MINOR}.$((PATCH + 1))" ;;
            esac
            BUMP="${{ inputs.version_bump }}"
          else
            # Automated: derive from conventional commits.
            # Capture output before eval — `eval $(cmd)` on its own returns
            # eval's own exit status (almost always 0), swallowing a nonzero
            # exit from compute_version_bump.sh (e.g. its fail-safe guard).
            BUMP_OUTPUT=$(bash scripts/compute_version_bump.sh)
            eval "$BUMP_OUTPUT"
            NEW="$TARGET_VERSION"
            BUMP="$BUMP_TYPE"
          fi

          echo "new=$NEW" >> "$GITHUB_OUTPUT"
          echo "bump_type=$BUMP" >> "$GITHUB_OUTPUT"
          echo "Version: $CURRENT -> $NEW (bump: $BUMP)"

      - name: Check final tag doesn't already exist
        if: steps.kind.outputs.kind != 'rc'
        run: |
          TAG="v${{ steps.version.outputs.new }}"
          if git tag -l "$TAG" | grep -q "^${TAG}$"; then
            echo "::error::Tag $TAG already exists"
            exit 1
          fi
          echo "Tag $TAG is available"

  # ── RC Release (staging push) ────────────────────────────────────────────────
  release-rc:
    name: "Pre-release RC v${{ needs.preflight.outputs.new_version }}"
    needs: preflight
    if: needs.preflight.outputs.release_kind == 'rc'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Compute RC number
        id: rc
        run: |
          TARGET="${{ needs.preflight.outputs.new_version }}"
          # Count existing rc tags for this target version
          RC_COUNT=$(git tag -l "v${TARGET}-rc.*" | wc -l | tr -d ' ')
          RC_NUM=$((RC_COUNT + 1))
          echo "rc_num=${RC_NUM}" >> "$GITHUB_OUTPUT"
          echo "rc_tag=v${TARGET}-rc.${RC_NUM}" >> "$GITHUB_OUTPUT"
          echo "RC tag will be: v${TARGET}-rc.${RC_NUM}"

      - name: Create annotated RC tag
        run: |
          RC_TAG="${{ steps.rc.outputs.rc_tag }}"
          TARGET="${{ needs.preflight.outputs.new_version }}"
          BUMP="${{ needs.preflight.outputs.bump_type }}"

          git tag -a "$RC_TAG" \
            -m "Pre-release ${RC_TAG}
          Target: v${TARGET} (${BUMP} bump)
          Branch: ${{ github.ref_name }}
          Commit: ${{ github.sha }}"

          git push origin "$RC_TAG"

      - name: Generate RC release notes
        id: notes
        run: |
          TARGET="${{ needs.preflight.outputs.new_version }}"
          BUMP="${{ needs.preflight.outputs.bump_type }}"
          RC_TAG="${{ steps.rc.outputs.rc_tag }}"

          # Find last final tag for commit range
          LAST_FINAL=$(git tag -l 'v*' \
            | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' \
            | sort -V | tail -1)

          NOTES="## Pre-release ${RC_TAG}

          **Target release:** v${TARGET} (${BUMP} bump)
          **Branch:** ${{ github.ref_name }}
          **Commit:** ${{ github.sha }}

          ### Commits since ${LAST_FINAL:-initial}
          "

          if [ -n "$LAST_FINAL" ]; then
            COMMIT_LOG=$(git log "${LAST_FINAL}..HEAD" --pretty=format:"- %s" | head -40)
            BREAKING=$(bash scripts/extract-breaking-changes.sh "$LAST_FINAL" HEAD || true)
          else
            COMMIT_LOG=$(git log --pretty=format:"- %s" | head -40)
            BREAKING=""
          fi

          if [ -n "$BREAKING" ]; then
            printf '%s\n\n%s\n\n%s' "$NOTES" "$BREAKING" "$COMMIT_LOG" > /tmp/rc_notes.md
          else
            printf '%s\n\n%s' "$NOTES" "$COMMIT_LOG" > /tmp/rc_notes.md
          fi
          echo "notes_file=/tmp/rc_notes.md" >> "$GITHUB_OUTPUT"

      - name: Create GitHub pre-release
        run: |
          gh release create "${{ steps.rc.outputs.rc_tag }}" \
            --title "${{ steps.rc.outputs.rc_tag }}" \
            --notes-file "${{ steps.notes.outputs.notes_file }}" \
            --prerelease \
            --target "${{ github.ref_name }}"
        env:
          GH_TOKEN: ${{ github.token }}

      - name: RC release summary
        run: |
          echo "## Pre-release ${{ steps.rc.outputs.rc_tag }}" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Item | Value |" >> "$GITHUB_STEP_SUMMARY"
          echo "|------|-------|" >> "$GITHUB_STEP_SUMMARY"
          echo "| RC Tag | ${{ steps.rc.outputs.rc_tag }} |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Target Version | v${{ needs.preflight.outputs.new_version }} |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Bump Type | ${{ needs.preflight.outputs.bump_type }} |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Branch | ${{ github.ref_name }} |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Commit | ${{ github.sha }} |" >> "$GITHUB_STEP_SUMMARY"

  # ── Final Release (main push) ─────────────────────────────────────────────────
  release-final:
    name: "Final Release v${{ needs.preflight.outputs.new_version }}"
    needs: preflight
    if: needs.preflight.outputs.release_kind == 'final'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Bump VERSION file
        run: |
          NEW="${{ needs.preflight.outputs.new_version }}"
          echo "$NEW" > VERSION
          # Update dist/VERSION if it is tracked
          if git ls-files --error-unmatch dist/VERSION 2>/dev/null; then
            echo "$NEW" > dist/VERSION
          fi

      - name: Update CHANGELOG.md
        run: |
          NEW="${{ needs.preflight.outputs.new_version }}"
          DATE=$(date +%Y-%m-%d)
          BUMP="${{ needs.preflight.outputs.bump_type }}"

          # Find last final tag for commit range (exclude the one we're about to create)
          LAST_FINAL=$(git tag -l 'v*' \
            | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' \
            | sort -V | tail -1)

          if [ -n "$LAST_FINAL" ]; then
            COMMITS=$(git log "${LAST_FINAL}..HEAD" --pretty=format:"- %s" \
              | grep -v "^- release: " | head -50)
            BREAKING=$(bash scripts/extract-breaking-changes.sh "$LAST_FINAL" HEAD || true)
          else
            COMMITS=$(git log --pretty=format:"- %s" \
              | grep -v "^- release: " | head -50)
            BREAKING=""
          fi

          # Build the entry in a temp file rather than interpolating COMMITS
          # (multi-line, and any commit subject containing |, &, \, or
          # newlines) into a sed s-command — that breaks with "unterminated
          # `s' command". Insert via awk instead: it reads the entry from a
          # file, so no commit content ever passes through a shell/sed
          # expression.
          ENTRY_FILE=$(mktemp)
          {
            echo "## v${NEW} (${DATE}) — ${BUMP} bump"
            echo ""
            if [ -n "$BREAKING" ]; then
              echo "$BREAKING"
              echo ""
            fi
            echo "$COMMITS"
          } > "$ENTRY_FILE"

          # Insert after the FIRST "---" separator only.
          awk -v entry="$ENTRY_FILE" '
            !done && /^---$/ {
              print
              print ""
              while ((getline line < entry) > 0) print line
              done = 1
              next
            }
            { print }
          ' CHANGELOG.md > CHANGELOG.md.new
          mv CHANGELOG.md.new CHANGELOG.md
          rm -f "$ENTRY_FILE"

      - name: Commit and tag
        run: |
          NEW="${{ needs.preflight.outputs.new_version }}"
          BUMP="${{ needs.preflight.outputs.bump_type }}"

          # Stage VERSION, dist/VERSION (if tracked), CHANGELOG
          git add VERSION CHANGELOG.md
          git ls-files --error-unmatch dist/VERSION 2>/dev/null && git add dist/VERSION || true

          git commit -m "release: v${NEW} [skip ci]

          Automated release commit.
          Bump: ${BUMP}
          Previous: v${{ needs.preflight.outputs.current_version }}
          CI: passed"

          git tag -a "v${NEW}" \
            -m "Release v${NEW} (${BUMP} bump)"

      - name: Push commit and tag
        run: |
          git push origin "${{ github.ref_name }}"
          git push origin "v${{ needs.preflight.outputs.new_version }}"

      - name: Generate final release notes
        id: notes
        run: |
          NEW="${{ needs.preflight.outputs.new_version }}"
          BUMP="${{ needs.preflight.outputs.bump_type }}"

          LAST_FINAL=$(git tag -l 'v*' \
            | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' \
            | sort -V | tail -2 | head -1)

          if [ -n "$LAST_FINAL" ]; then
            COMMITS=$(git log "${LAST_FINAL}..HEAD" --pretty=format:"- %s" \
              | grep -v "^- release: " | head -50)
            BREAKING=$(bash scripts/extract-breaking-changes.sh "$LAST_FINAL" HEAD || true)
          else
            COMMITS=$(git log --pretty=format:"- %s" \
              | grep -v "^- release: " | head -50)
            BREAKING=""
          fi

          {
            echo "## v${NEW} — ${BUMP} bump"
            echo ""
            if [ -n "$BREAKING" ]; then
              echo "$BREAKING"
              echo ""
            fi
            echo "$COMMITS"
          } > /tmp/release_notes.md

          echo "notes_file=/tmp/release_notes.md" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        run: |
          gh release create "v${{ needs.preflight.outputs.new_version }}" \
            --title "v${{ needs.preflight.outputs.new_version }}" \
            --notes-file "${{ steps.notes.outputs.notes_file }}" \
            --target "${{ github.ref_name }}"
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Final release summary
        run: |
          echo "## Release v${{ needs.preflight.outputs.new_version }}" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Item | Value |" >> "$GITHUB_STEP_SUMMARY"
          echo "|------|-------|" >> "$GITHUB_STEP_SUMMARY"
          echo "| Version | ${{ needs.preflight.outputs.current_version }} -> v${{ needs.preflight.outputs.new_version }} |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Bump | ${{ needs.preflight.outputs.bump_type }} |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Branch | ${{ github.ref_name }} |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Commit | ${{ github.sha }} |" >> "$GITHUB_STEP_SUMMARY"

  # ── Manual Release (workflow_dispatch) ───────────────────────────────────────
  release-manual:
    name: "Manual Release v${{ needs.preflight.outputs.new_version }}"
    needs:
      - preflight
    if: |
      always() &&
      needs.preflight.outputs.release_kind == 'manual' &&
      needs.preflight.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Bump version
        run: |
          echo "${{ needs.preflight.outputs.new_version }}" > VERSION
          if git ls-files --error-unmatch dist/VERSION 2>/dev/null; then
            echo "${{ needs.preflight.outputs.new_version }}" > dist/VERSION
          fi

      - name: Update changelog
        run: |
          NEW_VERSION="${{ needs.preflight.outputs.new_version }}"
          DATE=$(date +%Y-%m-%d)
          NOTES="${{ inputs.release_notes }}"

          # See the "Update CHANGELOG.md" step in release-final for why this
          # goes through a temp file + awk instead of an inline sed s-command
          # (release_notes can contain multi-line text or |, &, \).
          ENTRY_FILE=$(mktemp)
          {
            echo "## v${NEW_VERSION} (${DATE})"
            echo ""
            echo "$NOTES"
          } > "$ENTRY_FILE"

          awk -v entry="$ENTRY_FILE" '
            !done && /^---$/ {
              print
              print ""
              while ((getline line < entry) > 0) print line
              done = 1
              next
            }
            { print }
          ' CHANGELOG.md > CHANGELOG.md.new
          mv CHANGELOG.md.new CHANGELOG.md
          rm -f "$ENTRY_FILE"

      - name: Commit and tag
        run: |
          git add VERSION CHANGELOG.md
          git ls-files --error-unmatch dist/VERSION 2>/dev/null && git add dist/VERSION || true

          git commit -m "release: v${{ needs.preflight.outputs.new_version }} [skip ci]

          ${{ inputs.release_notes }}

          Bump: ${{ inputs.version_bump }}
          Previous: v${{ needs.preflight.outputs.current_version }}
          CI: passed

          git tag -a "v${{ needs.preflight.outputs.new_version }}" \
            -m "Release v${{ needs.preflight.outputs.new_version }}: ${{ inputs.release_notes }}"

      - name: Push
        run: |
          git push origin "${{ github.ref_name }}"
          git push origin "v${{ needs.preflight.outputs.new_version }}"

      - name: Create GitHub Release
        run: |
          gh release create "v${{ needs.preflight.outputs.new_version }}" \
            --title "v${{ needs.preflight.outputs.new_version }}" \
            --notes "${{ inputs.release_notes }}" \
            --target "${{ github.ref_name }}"
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Release summary
        run: |
          echo "## Manual Release v${{ needs.preflight.outputs.new_version }}" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Item | Value |" >> "$GITHUB_STEP_SUMMARY"
          echo "|------|-------|" >> "$GITHUB_STEP_SUMMARY"
          echo "| Version | ${{ needs.preflight.outputs.current_version }} -> ${{ needs.preflight.outputs.new_version }} |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Bump | ${{ inputs.version_bump }} |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Branch | ${{ github.ref_name }} |" >> "$GITHUB_STEP_SUMMARY"
          echo "| CI | passed |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Notes | ${{ inputs.release_notes }} |" >> "$GITHUB_STEP_SUMMARY"

  # ── Publish images to ghcr (final releases only) ──────────────────────────────
  # Two problems, one job:
  #   1. Until now, release.yml cut a GitHub release but never built/pushed
  #      container images — no ghcr images existed per release.
  #   2. The frontend bakes its displayed version (__APP_VERSION__, from the
  #      root VERSION file) at image-build time. Checking out the just-created
  #      release tag (VERSION already bumped by release-final/release-manual)
  #      and building from THIS commit makes the baked UI version equal the
  #      release tag instead of drifting from it.
  # Keyless cosign signing (Sigstore/Fulcio/Rekor via OIDC) needs no secrets
  # beyond the built-in GITHUB_TOKEN: id-token: write mints the OIDC token
  # cosign uses, packages: write lets it push to ghcr.io/<owner-of-this-repo>.
  release-publish:
    name: "Publish images v${{ needs.preflight.outputs.new_version }}"
    needs:
      - preflight
      - release-final
      - release-manual
    if: |
      always() &&
      (needs.preflight.outputs.release_kind == 'final' || needs.preflight.outputs.release_kind == 'manual') &&
      (needs.release-final.result == 'success' || needs.release-manual.result == 'success')
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      contents: read
      packages: write
      id-token: write
    env:
      # Derived from the repo owner so forks and mirrors publish to their own
      # namespace without editing this workflow.
      REGISTRY: ghcr.io/${{ github.repository_owner }}
      VERSION: ${{ needs.preflight.outputs.new_version }}
    steps:
      - name: Checkout release tag
        uses: actions/checkout@v6
        with:
          ref: v${{ needs.preflight.outputs.new_version }}
          fetch-depth: 0

      - name: Resolve release commit
        id: rev
        run: echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Install cosign
        uses: sigstore/cosign-installer@v3

      - name: Install syft
        uses: anchore/sbom-action/download-syft@v0

      - name: Build and push all images (docker-bake.hcl)
        run: docker buildx bake --push default
        env:
          REGISTRY: ${{ env.REGISTRY }}
          VERSION: ${{ env.VERSION }}
          PLATFORMS: linux/amd64,linux/arm64
          # OCI source label tracks whichever remote actually built the image.
          SOURCE_URL: ${{ github.server_url }}/${{ github.repository }}
          GIT_REVISION: ${{ steps.rev.outputs.sha }}
          ROLLING_TAG: latest

      - name: Sign, SBOM, and attest all images (keyless cosign)
        run: bash scripts/publish-signed-images.sh --execute
        env:
          BNK_FORGE_REGISTRY: ${{ env.REGISTRY }}
          BNK_FORGE_VERSION: ${{ env.VERSION }}

      - name: Publish summary
        run: |
          echo "## Published images v${{ needs.preflight.outputs.new_version }}" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Image | Tags |" >> "$GITHUB_STEP_SUMMARY"
          echo "|-------|------|" >> "$GITHUB_STEP_SUMMARY"
          for name in bnk-forge-api bnk-forge-worker bnk-forge-beat bnk-forge-frontend bnk-forge-proxy bnk-forge-mcp bnk-forge-operator; do
            echo "| ${name} | ${{ env.REGISTRY }}/${name}:${{ needs.preflight.outputs.new_version }}, :latest |" >> "$GITHUB_STEP_SUMMARY"
          done
