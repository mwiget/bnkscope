# API Configuration
# Leave empty or omit to use relative URLs (recommended for portability)
# Only set if you need to point to a different backend server
# VITE_API_URL=http://localhost:2651

# WebSocket Configuration (optional)
# Leave empty to auto-detect from window.location (recommended)
# VITE_WS_URL=ws://localhost:2651

# App Configuration
VITE_APP_NAME=bnkscope
VITE_APP_VERSION=2.10.39
